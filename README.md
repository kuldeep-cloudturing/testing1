# Word → Mandala (Streamlit)

## Run locally
1) Install Python 3
2) In Terminal, inside this folder:
   pip3 install -r requirements.txt
3) Set your key (do NOT put it in code):
   export OPENAI_API_KEY="YOUR_KEY"
4) Run:
   streamlit run app.py
